name: ga68-test

on:
  push:
  pull_request:

jobs:
  compile:
    runs-on: ubuntu-26.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install GCC Algol 68 front end
        run: |
          sudo apt-get update
          sudo apt-get install -y ga68-16
          command -v ga68-16
          ga68-16 --version

      - name: Discover generated Algol 68 sources
        run: |
          find . -type f \( -name '*.a68' -o -name '*.68' -o -name '*.alg' \) | sort

      - name: Compile discovered Algol 68 sources
        run: |
          set -e
          mapfile -t files < <(find . -type f \( -name '*.a68' -o -name '*.68' -o -name '*.alg' \) | sort)
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No Algol 68 sources found; skipping compile step."
            exit 0
          fi
          for f in "${files[@]}"; do
            echo "Compiling $f"
            ga68-16 -std=gnu68 -O2 -fstropping=upper "$f"
          done
